# Copy to .env.local
REPO_ROOT=..
CONTENT_ROOT=./data
AUTH_DISABLED=1
AUTH_SECRET=change-me-in-production
AUTH_PASSWORD=dichthuat
AUTH_USERS=admin:admin,translator:translator,reviewer:reviewer
# Machine translation uses the Cursor SDK (local agent, composer-2.5).
# Create a key at https://cursor.com/dashboard (API Keys section).
CURSOR_API_KEY=
# Optional model override (default: composer-2.5)
# CURSOR_MODEL=composer-2.5
